# Local Audio Files

Place all your `.mp3`, `.wav`, `.ogg`, or `.m4a` files in this directory. 

Because we are using Vite, the application will automatically read these files using `import.meta.glob` and display them in the Audio list! You do not need to manually specify the file paths anywhere in the code.

Just drop them in here and start the application.
